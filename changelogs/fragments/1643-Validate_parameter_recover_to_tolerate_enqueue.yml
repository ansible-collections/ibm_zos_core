bugfixes:
  - zos_backup_restore - Module would fail when recover option is True not allowing
    recoverable errors. Fix allow potentially recoverable errors should be ignored
    when recover is True.
    (https://github.com/ansible-collections/ibm_zos_core/pull/1643).