breaking_changes:
  - zos_lineinfile - Removes return value ``return_content`` in favor of ``stdout``.
    (https://github.com/ansible-collections/ibm_zos_core/pull/2253).

minor_changes:
  - zos_lineinfile - Adds alias ``after`` for option ``insertafter`` and alias ``before`` for option ``insertbefore``.
    Adds new return values ``stdout_lines`` and ``stderr_lines`` that returns any outstanding stdout or stderr message split in lines.
    (https://github.com/ansible-collections/ibm_zos_core/pull/2253).

trivial:
   - test_zos_lineinfile_func.py - Added a proper cleanup of a temporary file.
     (https://github.com/ansible-collections/ibm_zos_core/pull/2253).