trivial:
  - zos_operator - Update internal functions to account for the change to the
    unit of measurement of `timeout` now in centiseconds.
    (https://github.com/ansible-collections/ibm_zos_core/pull/1181).