minor_changes:
  - zos_job_query - Added address space used by jobs in return JSON
    as `job_type`.
    (https://github.com/ansible-collections/ibm_zos_core/pull/1673).
  - zos_job_output - Added address space used by jobs in return JSON
    as `job_type`.
    (https://github.com/ansible-collections/ibm_zos_core/pull/1673).
  - zos_job_submit - Added address space used by jobs in return JSON
    as `job_type`.
    (https://github.com/ansible-collections/ibm_zos_core/pull/1673).