bugfixes:
  - zos_apf - When trying to add a library into the APF list that was already added the module would fail. Fix now won't fail the module and inform the user that the library was already in the APF list.
    (https://github.com/ansible-collections/ibm_zos_core/pull/1893)